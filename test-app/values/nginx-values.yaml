# Nginx test application configuration
# Try to use Helm Chart default configuration, only override necessary settings

# Service type: LoadBalancer (for external access)
service:
  type: LoadBalancer

# Enable Prometheus Metrics Exporter (for monitoring)
metrics:
  enabled: true
  serviceMonitor:
    enabled: true
    namespace: monitoring
    labels:
      release: prometheus

